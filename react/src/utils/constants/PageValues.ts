// Converted from src/services/page.val.js

// Define the PageValues type based on the AngularJS value
type PageValuesType = {
  title: string | null;
  description: string | null;
  loading: boolean;
};

// Export the PageValues constant for use in React components
const PageValues: PageValuesType = {
  title: null,
  description: null,
  loading: false,
};

export default PageValues;
