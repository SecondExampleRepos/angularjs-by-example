interface PageValues {
  title: string | null;
  description: string | null;
  loading: boolean;
}

const PageValues: PageValues = {
  title: null,
  description: null,
  loading: false,
};

export default PageValues;
