// TypeScript constant for React components converted from AngularJS

// Define the type for PageValues
type PageValuesType = {
    title: string | null;
    description: string | null;
    loading: boolean;
};

// Define the PageValues constant
const PageValues: PageValuesType = {
    title: null,
    description: null,
    loading: false
};

export default PageValues;
